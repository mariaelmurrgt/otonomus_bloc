enum SignupStatus {
  initial,
  loading,
  success,
  failure,
}

enum LoginStatus { initial, loading, success, failure }

enum FetchPropertStatus {
  loading,
  success,
  failure,
}

enum AuthenticationStatus {
  unknown,
  authenticated,
  unauthenticated,
}
