enum AuthStatus {
  unkown,
  authenticated,
  unauthenticated,
}

enum SignupStatus {
  initial,
  loading,
  success,
  failure,
}

enum LoginStatus { initial, loading, success, failure }

enum FetchPropertStatus {
  loading,
  success,
  failure,
}
